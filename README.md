# archaeocode
A CLI tool for excavating the ancient history buried in any git repository. Color-coded commands reveal which files haven't been touched in years, how a file evolved commit by commit, and how old each line of code really is.
